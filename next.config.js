/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    domains: ['localhost' /* ,'example.com', 'anotherdomain.com' */], // Füge hier den Hostnamen hinzu, von dem aus du Bilder lädst
  },}

module.exports = nextConfig
